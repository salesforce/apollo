databaseChangeLog:
-  preConditions:
   -  runningAs:
         username: apollo
-  changeSet:
      id: 2
      author: hhildebrand
      changes:
      -  sql:
            splitStatements: true
            sql: CREATE TRIGGER IF NOT EXISTS DAG_DELETE AFTER DELETE ON DAG FOR EACH ROW CALL "com.salesforce.apollo.avalanche.Dag$DagDeleteTrigger";
