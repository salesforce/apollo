databaseChangeLog:
-  preConditions:
   -  runningAs:
         username: apollo
-  changeSet:
      id: 2
      author: hhildebrand
      changes:
      -  sql:
            splitStatements: false
            sql: "CREATE ALIAS PREFER AS $$ boolean prefer(byte[] entry, int depth, byte[] conflictSet, int confidence, byte[] preferred, int preferredConfidence, byte[] last) {return true;} $$;"